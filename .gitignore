node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# Variables d'environnement — copier .env.example vers .env
.env
.env*.local

# Réglages Claude Code propres à la machine (launch.json reste partagé)
.claude/settings.local.json

# Client Prisma généré — recréé par `prisma generate` (postinstall)
src/generated/

.DS_Store