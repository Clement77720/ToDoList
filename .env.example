# Copier ce fichier vers .env avant le premier `npm run db:reset`.
# SQLite en local ; en production, remplacer par une URL Postgres
# et changer le `provider` dans prisma/schema.prisma.
DATABASE_URL="file:./dev.db"
